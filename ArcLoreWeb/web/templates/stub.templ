package templates

// Stub is a placeholder page body for routes whose real handlers land in later
// steps. note identifies which step will replace it.
templ Stub(note string) {
	<section class="stub">
		<h1>ArcLore</h1>
		<p>{ note }</p>
	</section>
}
